import { Menu } from 'lucide-react';

const SideNav = () => {
  return (
    <div className=" flex flex-col gap-3">
      <div>
        <Menu />
      </div>
    </div>
  );
};

export default SideNav;
